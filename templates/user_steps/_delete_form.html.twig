<form method="post" action="{{ path('app_user_steps_delete', {'id': user_step.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_step.id) }}">
    <button class="btn">Delete</button>
</form>
