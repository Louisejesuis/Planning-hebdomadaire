{% extends 'base.html.twig' %}

{% block title %}Edit UserSteps{% endblock %}

{% block body %}
    <h1>Edit UserSteps</h1>

    {{ include('user_steps/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_steps_index') }}">back to list</a>

    {{ include('user_steps/_delete_form.html.twig') }}
{% endblock %}
